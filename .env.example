# Copy this to .env and fill in real values
# This file is safe to commit — .env is gitignored

# Auth
JWT_SECRET=replace-with-a-random-32-char-string

# GitHub OAuth (optional — needed for GitHub login)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Grafana
GRAFANA_PASSWORD=devops2024

# Kubernetes (only needed for cloud deploy)
# KUBECONFIG_B64=<base64 encoded ~/.kube/config>
